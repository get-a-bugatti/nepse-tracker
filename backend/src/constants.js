const urls = {
  NEPALSTOCK_URL: "https://nepalstock.com",
  API_AUTH_PROVE: "/api/authenticate/prove",
  API_NOTS_SECURITY: "/api/nots/security",
};

const others = {
  DB_NAME: "nepse_tracker",
  APP_NAME: "Cluster0",
};

export const { DB_NAME, APP_NAME } = others;
export const { NEPALSTOCK_URL, API_AUTH_PROVE, API_NOTS_SECURITY } = urls;
